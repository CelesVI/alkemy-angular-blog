import { SearchfiltertodoPipe } from './searchfiltertodo.pipe';

describe('SearchfiltertodoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchfiltertodoPipe();
    expect(pipe).toBeTruthy();
  });
});
